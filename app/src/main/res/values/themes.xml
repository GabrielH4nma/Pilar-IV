<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema Principal da Aplicação -->
    <style name="Theme.ProtocoloSombra" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- Tema da Splash Screen (Capa) -->
    <style name="Theme.ProtocoloSombra.Splash" parent="Theme.ProtocoloSombra">
        <!-- Método Antigo (Android 11 e inferior) -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowFullscreen">true</item>

        <!-- Método Novo (Android 12 / API 31+) -->
        <!-- Isto força a tua imagem a aparecer no centro em vez do ícone padrão -->
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="31">@drawable/cover_image</item>
        <item name="android:windowSplashScreenBackground" tools:targetApi="31">@color/black</item>
        <!-- Isto remove o círculo de fundo se a imagem for transparente -->
        <item name="android:windowSplashScreenIconBackgroundColor" tools:targetApi="31">@color/black</item>
    </style>
</resources>